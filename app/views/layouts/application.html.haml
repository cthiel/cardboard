!!!
%html
  %head
    %title CardBoard
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'
    = javascript_include_tag 'lib'
    = csrf_meta_tags
    = yield :head
  %body
    %nav{:role => "banner"}
      %h1.title
        =image_tag 'tomboy.png'
        CardBoard
      %span#notify
      %ul.actions
        / Add management (place for cusomization, import/export, etc.)
    %section.container
      = yield
