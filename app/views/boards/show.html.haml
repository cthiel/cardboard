- content_for :head do
  = javascript_include_tag 'dialog'
  = javascript_include_tag 'cardboard'
  = stylesheet_link_tag decks_path(:format => 'css'), :id => "decks_css"
#board
  .control.new_deck(title="Add a new deck") +
  #columns
